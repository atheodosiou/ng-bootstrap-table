/* tslint:disable:no-unused-variable */
import { BBodyDirective } from './b-body.directive';

describe('Directive: BBody', () => {
  it('should create an instance', () => {
    const directive = new BBodyDirective();
    expect(directive).toBeTruthy();
  });
});
